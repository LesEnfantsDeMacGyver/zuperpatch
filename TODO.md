# TODO

- Make the electrical reference voltage and recommended maximum cord load configurable instead of fixed at 230 V and 10 A.
- Make the assumed load for each desired free powerstrip socket configurable instead of fixed at 30 W.
